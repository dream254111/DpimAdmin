<script type="text/x-template" id="vue-component-mango-retrieve-modal">
    <div>
        <!-- Modal -->
        <div class="modal" tabindex="-1" role="dialog" aria-hidden="true" ref="modal1">
            <div class="modal-dialog modal-lg" role="document">
                <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title" id="exampleModalLabel">Modal title</h5>
                        <button type="button" class="close" aria-label="Close" v-on:click="closeModal">
                            <span aria-hidden="true">&times;</span>
                        </button>
                    </div>
                    <div class="modal-body">
                        {{test}}
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" v-on:click="closeModal">Close</button>
                        <button type="button" class="btn btn-primary">Save changes</button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</script>
<script>
    Vue.component('retrieve-modal', {
        template: '#vue-component-mango-retrieve-modal',
        data() {
            return {
                test: '',
                tableTemplate: [{ data_column: '', display_name: '', data_type: 'text', data_format: '', align: 'left' }]
            }
        },
        methods: {
            openModal() {
                $(this.$refs.modal1).modal('show');
            },
            closeModal() {
                $(this.$refs.modal1).modal('hide');
            },
            setData(d) {
                this.$set(this, 'test', d);
            }
        }
    });
</script>